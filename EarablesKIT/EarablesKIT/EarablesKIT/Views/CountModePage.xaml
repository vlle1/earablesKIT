<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:resource="clr-namespace:EarablesKIT.Resources;assembly=EarablesKIT"
             x:Class="EarablesKIT.Views.CountModePage">
    <ContentPage.Content>
        <StackLayout Padding="10" Spacing="60" Margin="0,30,0,0">
            <Label Text="CountMode"
                   Padding="20"
                   FontSize="Title"
                   HorizontalTextAlignment="Center"
                   TextColor="White"
                   BackgroundColor="CornflowerBlue"/>
            <StackLayout Spacing="10">
                <Label Text="Possible Activities"
                   Padding="20"
                   FontSize="Large"
                   HorizontalTextAlignment="Center"
                   BackgroundColor="PaleTurquoise"/>
                <StackLayout Padding="20">
                    <ListView x:Name="ActivityView" 
                          ItemsSource="{Binding PossibleActivities}" 
                          SelectedItem="{Binding SelectedActivity}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <TextCell Text="{Binding _name}"/>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackLayout>
            </StackLayout>
            <Button Text="Start Countmode" 
                    Clicked="OnStartButtonClicked"
                    VerticalOptions="CenterAndExpand" 
                    BackgroundColor="CornflowerBlue" 
                    BorderColor="Black" 
                    BorderWidth="1" 
                    CornerRadius="10" 
                    HorizontalOptions="Center"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>